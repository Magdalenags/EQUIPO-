[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = MOVGLG
	email = gutierrezmagdalena547@gmail.com
[alias]
	tree = log --graph --decorate --all --oneline
